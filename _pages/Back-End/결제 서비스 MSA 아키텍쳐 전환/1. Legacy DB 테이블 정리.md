---
title: "1. Legacy DB 테이블 정리"
date: "2025-08-31"
tags:
    - MySQL 8
    - Database Design
    - Schema Migration
    - MSA
---

> **스키마 분리를 통한 데이터 독립성 확보**
>
> 모놀리식 구조의 단일 스키마를 MSA 아키텍처에 맞게 Main Schema와 Payment Schema로 분리하는 과정을 다룹니다.

---

## 🎯 목표

### 현재 상황 (AS-IS)

```
┌─────────────────────────────────────┐
│        Main Schema (단일)            │
├─────────────────────────────────────┤
│ ✓ Users                             │
│ ✓ Orders                            │
│ ✓ Products                          │
│ ✓ tb_payments         ← 레거시      │
│ ✓ tb_payment_methods  ← 레거시      │
│ ✓ view_payment_info   ← VIEW        │
│ ✓ ...                               │
└─────────────────────────────────────┘
```

**문제점:**
- ❌ 결제 데이터가 다른 도메인과 혼재
- ❌ `tb_` 접두사의 레거시 네이밍
- ❌ 복잡한 VIEW로 인한 성능 저하
- ❌ 결제 서비스 독립 배포 불가능

### 목표 상태 (TO-BE)

```
┌─────────────────┐       ┌──────────────────┐
│  Main Schema    │       │ Payment Schema   │
├─────────────────┤       ├──────────────────┤
│ ✓ Users         │       │ ✓ transactions   │
│ ✓ Orders        │───┐   │ ✓ methods        │
│ ✓ Products      │   │   │ ✓ history        │
│                 │   │   │ ✓ pg_api_logs    │
│ Mapping Tables: │   └──→│                  │
│ ✓ order_payment │       │ (순수 PG 데이터) │
│ ✓ payment_state │       │                  │
└─────────────────┘       └──────────────────┘
```

**기대 효과:**
- ✅ 명확한 데이터 소유권
- ✅ 독립적인 스키마 관리
- ✅ 결제 서비스 독립 배포
- ✅ 데이터베이스 접근 제어

---

## 🗑️ Phase 1: Legacy 테이블 제거

### 1️⃣ 레거시 결제 테이블 Drop

```sql
-- 1. 백업 먼저! (중요)
CREATE TABLE backup_tb_payments AS SELECT * FROM tb_payments;
CREATE TABLE backup_tb_payment_methods AS SELECT * FROM tb_payment_methods;

-- 2. 의존성 확인
SELECT
  TABLE_NAME,
  CONSTRAINT_NAME,
  REFERENCED_TABLE_NAME
FROM information_schema.KEY_COLUMN_USAGE
WHERE REFERENCED_TABLE_NAME IN ('tb_payments', 'tb_payment_methods')
  AND TABLE_SCHEMA = 'main_db';

-- 3. 외래 키 제약 조건 제거
ALTER TABLE orders DROP FOREIGN KEY fk_orders_payment;

-- 4. 레거시 테이블 삭제
DROP TABLE IF EXISTS tb_payments;
DROP TABLE IF EXISTS tb_payment_methods;
DROP TABLE IF EXISTS tb_payment_history;
```

#### ⚠️ 주의사항

- 📦 **반드시 백업**: 운영 데이터는 먼저 백업 필수
- 🔗 **의존성 체크**: 외래 키 관계 확인 후 제거
- 📊 **데이터 마이그레이션**: 기존 데이터를 신규 구조로 이관 계획 수립

### 2️⃣ VIEW 테이블 제거

```sql
-- VIEW 목록 확인
SHOW FULL TABLES IN main_db WHERE TABLE_TYPE = 'VIEW';

-- 결제 관련 VIEW 삭제
DROP VIEW IF EXISTS view_payment_info;
DROP VIEW IF EXISTS view_payment_summary;
DROP VIEW IF EXISTS view_transaction_detail;
```

**VIEW 제거 이유:**
- 복잡한 JOIN으로 인한 성능 저하
- 변경 영향 범위 파악 어려움
- API 레벨에서 데이터 조합으로 대체

---

## 🏗️ Phase 2: 신규 스키마 및 테이블 생성

### 1️⃣ Payment Schema 생성

```sql
-- 독립된 결제 전용 스키마 생성
CREATE DATABASE IF NOT EXISTS payment_db
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

-- 결제 서비스 전용 사용자 생성
CREATE USER 'payment_service'@'%' IDENTIFIED BY 'secure_password';
GRANT ALL PRIVILEGES ON payment_db.* TO 'payment_service'@'%';
FLUSH PRIVILEGES;
```

### 2️⃣ Payment Schema 테이블 설계

#### 📄 결제 트랜잭션 테이블

```sql
-- 결제 요청/응답 원시 데이터 저장
CREATE TABLE payment_db.transactions (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  pg_transaction_id VARCHAR(100) UNIQUE NOT NULL COMMENT 'PG사 트랜잭션 ID',
  pg_company VARCHAR(50) NOT NULL COMMENT 'PG사 (TOSS, NICE, etc.)',
  transaction_type VARCHAR(20) NOT NULL COMMENT 'PAYMENT, REFUND, CANCEL',
  amount DECIMAL(15,2) NOT NULL COMMENT '결제 금액',
  currency VARCHAR(3) DEFAULT 'KRW' COMMENT '통화',
  status VARCHAR(20) NOT NULL COMMENT 'PENDING, SUCCESS, FAILED',

  -- PG사 원본 데이터 (가공하지 않음)
  pg_request_data JSON COMMENT 'PG사 요청 원본',
  pg_response_data JSON COMMENT 'PG사 응답 원본',

  -- 메타 정보
  requested_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  completed_at TIMESTAMP NULL,

  -- 인덱스
  INDEX idx_pg_transaction_id (pg_transaction_id),
  INDEX idx_status (status),
  INDEX idx_requested_at (requested_at)
) COMMENT '결제 트랜잭션 원시 데이터';
```

#### 💳 결제 수단 테이블

```sql
-- 등록된 결제 수단 정보
CREATE TABLE payment_db.methods (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  pg_billing_key VARCHAR(100) UNIQUE NOT NULL COMMENT 'PG사 빌링키',
  pg_company VARCHAR(50) NOT NULL COMMENT 'PG사',
  method_type VARCHAR(20) NOT NULL COMMENT 'CARD, ACCOUNT, MOBILE',

  -- 카드 정보 (일부만 저장)
  card_company VARCHAR(50) COMMENT '카드사',
  card_last4 VARCHAR(4) COMMENT '카드 마지막 4자리',

  -- PG사 원본 데이터
  pg_request_data JSON COMMENT '등록 요청 데이터',
  pg_response_data JSON COMMENT '등록 응답 데이터',

  status VARCHAR(20) NOT NULL DEFAULT 'ACTIVE' COMMENT 'ACTIVE, INACTIVE, DELETED',
  registered_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_at TIMESTAMP NULL,

  INDEX idx_pg_billing_key (pg_billing_key),
  INDEX idx_status (status)
) COMMENT '결제 수단 원시 데이터';
```

#### 📊 결제 이력 테이블

```sql
-- 결제 상태 변경 이력
CREATE TABLE payment_db.transaction_history (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  transaction_id BIGINT NOT NULL,
  previous_status VARCHAR(20),
  current_status VARCHAR(20) NOT NULL,
  change_reason VARCHAR(255),
  changed_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,

  FOREIGN KEY (transaction_id) REFERENCES transactions(id),
  INDEX idx_transaction_id (transaction_id),
  INDEX idx_changed_at (changed_at)
) COMMENT '결제 상태 변경 이력';

-- 결제 수단 상태 변경 이력
CREATE TABLE payment_db.method_history (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  method_id BIGINT NOT NULL,
  previous_status VARCHAR(20),
  current_status VARCHAR(20) NOT NULL,
  change_reason VARCHAR(255),
  changed_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,

  FOREIGN KEY (method_id) REFERENCES methods(id),
  INDEX idx_method_id (method_id),
  INDEX idx_changed_at (changed_at)
) COMMENT '결제 수단 상태 변경 이력';
```

#### 🔍 API 통신 로그 테이블

```sql
-- PG사 API 호출 로그 (디버깅 및 모니터링용)
CREATE TABLE payment_db.pg_api_logs (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  pg_company VARCHAR(50) NOT NULL,
  api_endpoint VARCHAR(255) NOT NULL COMMENT 'API URL',
  http_method VARCHAR(10) NOT NULL COMMENT 'GET, POST, PUT, DELETE',

  -- 요청/응답 저장 (JSON 형태)
  request_headers JSON,
  request_body JSON,
  response_status INT COMMENT 'HTTP 상태 코드',
  response_headers JSON,
  response_body JSON,

  -- 성능 측정
  duration_ms INT COMMENT '응답 시간 (밀리초)',

  -- 메타 정보
  success BOOLEAN DEFAULT FALSE,
  error_message TEXT,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,

  INDEX idx_pg_company (pg_company),
  INDEX idx_success (success),
  INDEX idx_created_at (created_at)
) COMMENT 'PG사 API 통신 로그';
```

---

### 3️⃣ Main Schema 매핑 테이블 설계

#### 🔗 주문-결제 매핑 테이블

```sql
-- Main Schema에 생성
CREATE TABLE main_db.order_payment_mapping (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,

  -- Main Schema 참조
  order_id BIGINT NOT NULL COMMENT '주문 ID',

  -- Payment Schema 참조 (ID만)
  payment_transaction_id BIGINT NOT NULL COMMENT 'Payment Schema의 transactions.id',

  -- 도메인별 상태 관리
  payment_status VARCHAR(20) NOT NULL COMMENT '주문 관점의 결제 상태',
  payment_confirmed_at TIMESTAMP NULL COMMENT '결제 확정 시간',

  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (order_id) REFERENCES orders(id),
  UNIQUE KEY uk_order_payment (order_id, payment_transaction_id),
  INDEX idx_payment_status (payment_status)
) COMMENT '주문-결제 매핑 (Main Schema 관리)';
```

#### 📋 결제 상태 추적 테이블

```sql
-- 최근 결제 상태만 저장 (빠른 조회용)
CREATE TABLE main_db.payment_states (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  order_id BIGINT NOT NULL UNIQUE,

  -- 결제 상태 스냅샷
  current_status VARCHAR(20) NOT NULL,
  payment_amount DECIMAL(15,2) NOT NULL,
  paid_amount DECIMAL(15,2) DEFAULT 0,
  refunded_amount DECIMAL(15,2) DEFAULT 0,

  -- Payment Schema 참조
  last_transaction_id BIGINT COMMENT '마지막 트랜잭션 ID',

  last_updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (order_id) REFERENCES orders(id),
  INDEX idx_current_status (current_status)
) COMMENT '주문별 결제 상태 (캐시성)';
```

#### 💰 사용자-결제수단 매핑 테이블

```sql
-- 사용자와 결제 수단 연결
CREATE TABLE main_db.user_payment_method_mapping (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,

  -- Payment Schema 참조
  payment_method_id BIGINT NOT NULL COMMENT 'Payment Schema의 methods.id',

  is_default BOOLEAN DEFAULT FALSE COMMENT '기본 결제 수단 여부',
  is_active BOOLEAN DEFAULT TRUE,

  registered_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_at TIMESTAMP NULL,

  FOREIGN KEY (user_id) REFERENCES users(id),
  INDEX idx_user_id (user_id),
  INDEX idx_is_default (is_default)
) COMMENT '사용자-결제수단 매핑';
```

---

## 📊 데이터 흐름 다이어그램

### 결제 요청 시

```
1. Order Service (Main DB)
   └→ order_payment_mapping 생성

2. Payment Service (Payment DB)
   └→ transactions 생성 (PG사 API 호출)
   └→ pg_api_logs 기록

3. Order Service (Main DB)
   └→ payment_states 업데이트 (캐시)
```

### 조회 시

```
조회 요청
  ↓
Main DB: payment_states 조회 (빠른 응답)
  ↓
필요 시: Payment DB에서 상세 정보 조회
```

---

## 🔐 접근 권한 설계

### Payment Schema 접근 제어

```sql
-- Payment Service만 접근 가능
GRANT ALL PRIVILEGES ON payment_db.* TO 'payment_service'@'%';

-- Main Service는 읽기만 가능 (필요 시)
GRANT SELECT ON payment_db.transactions TO 'main_service'@'%';

-- 일반 서비스는 접근 불가
REVOKE ALL PRIVILEGES ON payment_db.* FROM 'other_service'@'%';
```

---

## 💡 설계 원칙 정리

### 1️⃣ 순수 데이터만 저장 (Payment Schema)

```sql
-- ✅ Good: PG사 원본 데이터 저장
pg_request_data JSON,
pg_response_data JSON

-- ❌ Bad: 도메인 로직 데이터 혼재
user_name VARCHAR(100),  -- 이건 Main Schema에!
```

### 2️⃣ 느슨한 결합 (ID 참조만)

```sql
-- ✅ Good: ID만 참조
payment_transaction_id BIGINT

-- ❌ Bad: 외래 키로 강한 결합
FOREIGN KEY (payment_id) REFERENCES payment_db.transactions(id)
-- 크로스 스키마 FK는 MSA에서 지양
```

### 3️⃣ 캐시 테이블 활용

```sql
-- payment_states: 빠른 조회를 위한 스냅샷
-- 자주 조회되는 정보만 캐싱
```

---

## 📝 체크리스트

### 작업 전

- [ ] 백업 계획 수립
- [ ] 롤백 시나리오 작성
- [ ] 의존성 분석 완료
- [ ] 다운타임 계획 수립

### 작업 중

- [ ] 레거시 테이블 백업 완료
- [ ] Payment Schema 생성 완료
- [ ] 신규 테이블 생성 완료
- [ ] 매핑 테이블 생성 완료
- [ ] 인덱스 설정 완료

### 작업 후

- [ ] 데이터 마이그레이션 계획 수립
- [ ] 접근 권한 설정 완료
- [ ] API 코드 업데이트 완료
- [ ] 통합 테스트 완료

---

## 🚀 다음 단계

[2. 어댑터 패턴 설계](/Back-End/결제-서비스-MSA-아키텍쳐-전환/2-어댑터-패턴-설계)로 이동하여 PG사 연동을 위한 추상화 작업을 진행합니다.

**데이터베이스 분리 완료! 이제 애플리케이션 코드 설계로!** 💪